# Set your identity (only once)
git config user.email "your-email@example.com"
git config user.name "Your Name"

# Initialize and Commit
git init
git add .
git commit -m "Initial commit: Luxury Meraki Salon Website"

# Link to GitHub and Push
git branch -M main
git remote add origin https://github.com/YOUR_GITHUB_USERNAME/meraki-salon.git
git push -u origin main
